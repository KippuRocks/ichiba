name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  # Metadata object storage for the test API: MinIO, as kippu-api's CI runs it.
  # RELEASE.2025-09-07T16-13-09Z, by digest (the multi-architecture index), from quay.io.
  MINIO_IMAGE_DIGEST: sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
  MINIO_IMAGE: quay.io/minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
  MINIO_LOCAL_TAG: ichiba-ci/minio:pinned

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint, typecheck, test, build (Node 24)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      # No user-visible string uses fee vocabulary (REQ-SP-1a) or claims trustless,
      # tamper-proof or decentralised properties (REQ-TM-2).
      - run: pnpm lint:copy
      - name: A forbidden term added to a string fails the copy lint
        run: |
          for term in gas trustless; do
            cp src/app/page.tsx "$RUNNER_TEMP/page.tsx"
            printf '\nexport const probe = "No %s";\n' "$term" >> src/app/page.tsx
            if pnpm --silent lint:copy; then
              echo "the copy lint accepted \"$term\"" >&2
              exit 1
            fi
            cp "$RUNNER_TEMP/page.tsx" src/app/page.tsx
          done
      - run: pnpm build

  e2e:
    name: Preview — the production build under next start, end to end against kippu-api (Chromium)
    runs-on: ubuntu-latest
    services:
      kippu-store:
        # The test API's Kippu store: its own PostgreSQL instance and credentials.
        image: postgres:18-alpine
        env:
          POSTGRES_USER: kippu_api
          POSTGRES_PASSWORD: kippu_api_ci
          POSTGRES_DB: kippu_api
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U kippu_api -d kippu_api"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 30
    env:
      KIPPU_DATABASE_URL: postgres://kippu_api:kippu_api_ci@127.0.0.1:5432/kippu_api
      # A local stand-in: no object store, CDN or DNS record exists yet.
      KIPPU_METADATA_S3_BUCKET: kippu-metadata
      KIPPU_METADATA_S3_ENDPOINT: http://127.0.0.1:9000
      KIPPU_METADATA_S3_FORCE_PATH_STYLE: "true"
      KIPPU_METADATA_S3_ACCESS_KEY_ID: kippu_metadata
      KIPPU_METADATA_S3_SECRET_ACCESS_KEY: kippu_metadata_ci
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      # A service container cannot be given MinIO's `server` command, so it is started here.
      # The image is kept in the Actions cache; quay.io is reached only on a miss, with retries.
      - name: Restore the MinIO image from the cache
        id: minio-image
        uses: actions/cache@v6
        with:
          path: /tmp/minio-image.tar
          key: minio-image-${{ env.MINIO_IMAGE_DIGEST }}
      - name: Pull the MinIO image on a cache miss
        if: steps.minio-image.outputs.cache-hit != 'true'
        run: |
          for attempt in 1 2 3 4 5; do
            if docker pull "$MINIO_IMAGE"; then break; fi
            if [ "$attempt" = 5 ]; then echo "quay.io did not serve $MINIO_IMAGE" >&2; exit 1; fi
            sleep $((attempt * 15))
          done
          # `docker load` does not restore a repository digest, so the cached image is tagged.
          docker tag "$MINIO_IMAGE" "$MINIO_LOCAL_TAG"
          docker save "$MINIO_LOCAL_TAG" -o /tmp/minio-image.tar
      - name: Start metadata object storage (MinIO) with its bucket
        run: |
          if ! docker image inspect "$MINIO_LOCAL_TAG" >/dev/null 2>&1; then
            docker load -i /tmp/minio-image.tar
          fi
          docker run -d --name kippu-metadata-storage -p 9000:9000 \
            -e MINIO_ROOT_USER=kippu_metadata -e MINIO_ROOT_PASSWORD=kippu_metadata_ci \
            "$MINIO_LOCAL_TAG" server /data
          for _ in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:9000/minio/health/live; then break; fi
            sleep 1
          done
          docker exec kippu-metadata-storage sh -c \
            'mc alias set local http://127.0.0.1:9000 kippu_metadata kippu_metadata_ci && mc mb local/kippu-metadata'
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm e2e
      - if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7

  vendor:
    name: Vendored @kippu/api reproduces from its pinned commit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: pnpm vendor:check
